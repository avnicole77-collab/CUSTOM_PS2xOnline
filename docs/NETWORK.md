# Network

This module handles host/guest room connectivity, NAT status, ping checks, and room code generation.

Safety constraints:
- Do not store PINs in plain text
- Verify network capability before join
